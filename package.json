{
  "name": "ROC",
  "version": "1.0.0",
  "license": "GPL-3.0-or-later",
  "private": true,
  "workspaces": [
    "ROC-Client",
    "ROC-Server"
  ],
  "scripts": {
    "client": "yarn workspace ROC-Client run build && yarn workspace ROC-Client run start",
    "server": "yarn workspace ROC-Server run start"
  },
  "engines": {
    "node": ">=16",
    "yarn": ">=1.22.0"
  }
}
